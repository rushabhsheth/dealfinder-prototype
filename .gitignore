node_modules
dist
dist-ssr
*.local
.DS_Store
.~lock.*

# Business docs — kept out of the repo (PRD, financial model, thesis, catalogs)
PRDs/
.vercel

# Machine-local Claude Code settings
.claude/settings.local.json

# Server secrets — never commit env files (only *.example is tracked)
.env
.env.*
!.env.example
server/.env
server/.env.*
!server/.env.example
